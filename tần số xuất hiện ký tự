#include <bits/stdc++.h>
#include <iostream>
using namespace std;

int main()
{
    int soLuong, k;
    string str[100];
    list<string> list;
    cin >> soLuong >> k;
    for (int i = 0; i < soLuong; i++)
    {
        cin >> str[i];
    }
    int dem = 0;
    for (int i = 0; i < soLuong; i++)
    {
        for (int j = 1; j < soLuong; j++)
        {
            if (str[i] == str[j])
            {
                dem++;
                if (dem == k)
                {
                    cout << str[i];
                    list.push_back(str[i]);
                    i++;
                }
            }
        }
    }
    if (list.size() == 0)
    {
        cout << "-1";
    }
    system("pause");
    return 0;
}
