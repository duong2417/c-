#include <bits/stdc++.h>
#include <iostream>
using namespace std;

int main()
{
   string str="";
   cin>>str;
   int l = str.length(), dem=0;
   for (int i=0;i<l;i++){
       if (str[i]==toupper(str[i]))
       dem++;
   }
   cout<<dem;
    system("pause");
    return 0;
}
